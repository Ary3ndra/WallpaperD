# Copy to keystore.properties (gitignored) and fill in. Used only for local/CI release signing.
# F-Droid ignores this entirely and signs with its own key.
storeFile=/absolute/path/to/wallchanger.jks
storePassword=CHANGEME
keyAlias=wallchanger
keyPassword=CHANGEME
